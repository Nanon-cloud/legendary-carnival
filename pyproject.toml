[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "thai-drill-backend"
version = "0.1.0"
dependencies = [
    "fastapi==0.95.0",
    "uvicorn==0.21.0",
    "psycopg2-binary==2.9.7",
    "pydantic==1.10.12"
]
